*~

# ── Claude Code ────────────────────────────────────────────
# Context file for Claude Code sessions (not part of project)
/CLAUDE.md
.claude

# ── Working notes ──────────────────────────────────────────
/Updates/

# ── Allophone audio (internal build tool, not for SD card) ──
/Alofones/
/SD Content/SYS/VOICE/

# ── Arduino / STM32duino ───────────────────────────────────
# Old firmware versions archive (kept locally, not in repo)
/Arduino/bak/
# Build output
**/build/
**/.theia/
*.elf
*.eep
*.d

# ── Eagle PCB ──────────────────────────────────────────────
# Schematic and board files (kept in private repo)
*.brd
*.sch
# Old versions archive (kept locally, not in repo)
/Circuit/BAK/
# Auto-generated backup files (created on every save)
*.b##
*.b#[0-9]
*.s##
*.s#[0-9]
*.l#[0-9]
# CAM / DRC output (generated from .brd)
*.ctl
*.mnb
*.mnt
# Eagle scripts and generated reports
/Circuit/*.scr
/Circuit/partlist_smd.txt

# ── Xilinx ISE (Spartan-6 FPGA) ────────────────────────────
# Old versions archive (kept locally, not in repo)
/FPGA/BAK/
# Reports and logs
*.bgn
*.bld
*.cmd_log
*.debug
*.drc
*.lso
*.lst
*.log
*.mrp
*.ngr
*.out
*.pad
*.par
*.twr
*.twx
*.syr
*.xreport
*.xrpt
*.xmsgs
*.xwbt
*.ptwx
*.stx
# Intermediate netlist and implementation files
*.ngc
*.ngd
*.ncd
*.ngm
*.bmm
*.xdl
*.map
*.sym
*.xpi
*.xbcd
# Core Generator / IP generated
*.veo
*.vdbl
*.vdbx
*.sdbl
*.sdbx
*.cgc
*.cgp
*.ipf
*.cfi
*.cdc
*.ncf
*.ut
*.pcf
*.prm
*.gise
*.projectmgr
*.do
*.in
*.key
# Xilinx temporary directories
**/_ngo/
**/_xmsgs/
**/xlnx_auto_0_xdb/
**/.Xil/
**/SD81_html/
**/xst/work/
**/xst/dump.xst/
**/iseconfig/

# ── Z80 ROM ────────────────────────────────────────────────
/z80rom/sdmodrom.o
/z80rom/sdmodrom.bin
/z80rom/SDBOOST.ROM

# ── Obsolete docs (superseded by MANUAL/) ──────────────────
/obsoleto/
# Old STM32 firmware version, superseded by SD81BoosterV2_039_STM32
/Arduino/SD81BoosterV2_038_STM32/

# ── 3D-printable case files (kept locally, not in repo) ────
/STL/

# ── Dev-only scratch tests, not release content ─────────────
/test/

# Old VGM test files superseded by the curated set in EXAMPLES/VGM
/EXAMPLES/P/VGM/
/EXAMPLES/P/CARTOONS/

# ── Release binaries ────────────────────────────────────────
# Firmware update goes via SD card, not via the repo; the update
# scripts/binaries are attached to each Codeberg release instead.
/FIRMWARE/
# SD card reference content is attached to each release instead.
/SD Content/
/SD Content.zip
# Stray FPGA bitstream / flashing tool copies
/FPGA/SD81V2.1000/sd81.mcs
/FPGA/SD81V2.1000/sd81.mcs.bak
/Arduino/flash.zip
/Arduino/flash/
